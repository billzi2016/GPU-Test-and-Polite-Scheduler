# GPU-Test-and-Polite-Scheduler - 产品需求文档（PRD）

## 1. 文档信息

- 项目名称：`GPU-Test-and-Polite-Scheduler`
- 文档类型：产品需求文档（PRD）
- 文档版本：v1.0
- 目标平台：Linux GPU 服务器
- 面向用户：共享 GPU 服务器中的开发者、研究者、训练/采样任务维护者

---

## 2. 项目概述

`GPU-Test-and-Polite-Scheduler` 是一个面向多用户共享 GPU 环境的轻量级工具集，目标是同时解决两类常见问题：

1. GPU 是否可用、稳定、多卡通信是否正常，缺乏统一且快速的验证方式。
2. 长时间运行任务依赖人工值守，容易因进程退出、SSH 断连或与他人抢占 GPU 资源而中断。

本项目提供两大核心能力：

- GPU 基础状态检查、压力测试与多卡通信测试
- 基于 `tmux` 的后台任务守护与“礼貌型”自动调度机制

项目强调以下原则：

- 开箱即用
- 尽量少依赖复杂基础设施
- 对共享环境友好
- 支持任务异常退出后的断点续跑

---

## 3. 背景与问题定义

在实验室、团队服务器或共享算力节点中，GPU 的使用通常存在以下痛点：

### 3.1 硬件与环境状态不透明

用户在开始训练或采样前，往往需要先确认：

- CUDA 是否安装正确
- 驱动版本是否可用
- GPU 是否可见
- 高负载下是否存在散热、掉卡、报错等问题
- 多卡通信链路是否正常

缺少统一工具时，用户通常依赖零散命令和临时脚本，效率低且结果不一致。

### 3.2 长任务运行依赖人工维护

长周期任务通常存在以下风险：

- SSH 断开后前台任务丢失
- 程序因偶发错误退出后无人发现
- 目标 GPU 被别人占用时任务继续强行启动，造成资源冲突
- 任务没有断点续传能力，进程退出后需要从头开始

这些问题会显著降低共享 GPU 服务器的使用效率。

---

## 4. 产品目标

### 4.1 核心目标

本项目需要实现以下目标：

1. 提供统一的 GPU 可用性验证入口
2. 提供可持续运行的 GPU 压力测试能力
3. 提供多卡通信连通性与带宽验证能力
4. 提供基于 `tmux` 的后台任务托管方案
5. 提供“空闲就跑、有人就让、挂了可恢复”的礼貌型调度能力
6. 引导用户将自己的任务改造为支持断点续传的原子任务

### 4.2 非目标

以下内容不属于本阶段范围：

- 不实现集群级资源调度系统
- 不替代 Slurm、K8s、Ray 等成熟调度平台
- 不做 Web UI
- 不做跨机器统一调度
- 不负责模型训练逻辑本身
- 不保证对所有深度学习框架做完全兼容

---

## 5. 目标用户

### 5.1 主要用户

- 共享 GPU 服务器的个人开发者
- 使用单机多卡环境的研究人员
- 需要长时间运行训练、采样、推理任务的用户

### 5.2 用户特征

- 熟悉 Linux 基本命令行操作
- 能够使用 Python / Bash 启动任务
- 能理解 GPU 编号、显存、利用率等基本概念
- 希望减少人工值守成本，但不希望引入复杂平台

---

## 6. 典型使用场景

### 场景一：新机器验卡

用户拿到一台新服务器后，希望快速验证：

- CUDA 和驱动是否正常
- 每张卡能否被识别
- GPU 是否能稳定跑满
- 多卡互联是否正常

### 场景二：夜间长时间采样任务

用户有一组需要跑几个小时到几天的采样任务，希望：

- 任务在 `tmux` 中后台运行
- SSH 断开后不影响任务
- 进程退出后自动恢复
- 若目标卡被别人占用，则先暂停等待，空闲后再继续

### 场景三：共享环境礼貌让步

用户希望自己的后台任务不要和别人抢 GPU：

- 如果发现目标卡已有明显负载，则不启动
- 如果任务中断后准备重启，也先检查该卡是否被别人使用
- 只有卡空闲时才重新拉起任务

---

## 7. 功能需求

---

## 7.1 模块一：GPU 基础状态与压力测试

### 7.1.1 功能：环境基础打印

#### 需求说明

系统提供一个基础检查入口，用于打印当前 GPU 运行环境信息。

#### 输入

- 无或可选指定 GPU 列表

#### 输出

至少包含以下内容：

- `nvidia-smi` 关键信息或等价摘要
- GPU topo 信息，如 `nvidia-smi topo -m` 输出
- CUDA 版本
- GPU 驱动版本
- 可见 GPU 列表
- 每张 GPU 的编号、名称、显存信息
- 当前机器是否具备运行测试所需的基本依赖

#### 验收标准

- 用户运行后能在单次输出中看到完整环境概况
- 用户能直接看到当前 GPU 状态和多卡拓扑关系，无需再手动补查
- 输出应足够清晰，便于截图或复制给他人排查问题

---

### 7.1.2 功能：GPU 压力测试（Burn-in Test）

#### 需求说明

系统提供一个压力测试入口，对指定 GPU 执行持续高负载矩阵计算任务，以验证算力、散热与稳定性。

#### 功能要求

- 支持指定一张或多张 GPU
- 启动测试前打印当前 `nvidia-smi` 信息，便于用户确认目标卡状态
- 每张 GPU 启动独立计算任务
- 压测需包含明确同步操作，避免仅提交算子但没有真实打满 GPU
- 支持长时间持续运行，直到用户手动中断
- 可定期输出运行状态，如迭代次数、耗时、设备信息

#### 约束

- 压测行为应尽可能使 `nvidia-smi` 中的 `GPU-Util` 接近 100%
- 多卡并行时不应互相阻塞
- 允许用户控制矩阵规模或负载强度，以适配不同显存容量的设备

#### 验收标准

- 指定 GPU 在 `nvidia-smi` 中可观察到持续高利用率
- 程序连续运行一段时间后不因同步错误、OOM 或设备异常而退出
- 用户能够通过该测试观察温度、功耗、频率和稳定性表现

---

### 7.1.3 功能：多卡通信测试

#### 需求说明

系统提供一个独立入口，用于测试多张 GPU 之间的基础通信连通性和通信性能。

#### 功能要求

- 支持选择参与测试的 GPU 集合
- 测试前打印 GPU topo 信息，便于用户确认参与测试设备之间的连接关系
- 支持执行多卡通信测试任务
- 支持输出通信耗时、有效带宽或相关统计指标
- 应明确报告测试是否成功以及是否存在异常卡或异常链路

#### 验收标准

- 用户能快速判断多卡是否可以协同工作
- 测试输出可用于发现明显的通信异常或带宽瓶颈
- 多卡测试失败时，系统应能给出可定位问题的错误信息

---

## 7.2 模块二：智能断点调度与守护系统

### 7.2.1 功能：原子化断点任务规范

#### 需求说明

项目需要提供一套示例规范，指导用户将自己的任务脚本改造成“可中断、可恢复、可安全保存”的原子任务。

#### 功能要求

- 说明任务应如何定期保存 checkpoint
- 说明保存过程应避免写入半成品文件
- 说明任务启动时如何自动检测并恢复最近一次有效进度
- 提供任务状态组织建议，如输出目录、日志、checkpoint 命名约定

#### 验收标准

- 用户可基于示例快速改造自己的训练或采样脚本
- 任务被中断后，下一次重启可以继续执行而不是从头开始
- 不会因异常中断造成 checkpoint 文件损坏

---

### 7.2.2 功能：基于 `tmux` 的多实例部署

#### 需求说明

用户可以定义多个任务，系统自动为每个任务创建独立的 `tmux` session，并下发对应命令。

#### 功能要求

- 支持从配置文件读取任务列表
- 每个任务至少包含：
  - 任务名称
  - 目标 GPU 编号
  - 启动命令
  - 工作目录或日志目录
- 每个任务对应一个独立 `tmux` session
- 支持查看当前已部署任务的 session 信息

#### 验收标准

- 用户一次启动后，多任务被正确分发到不同 `tmux` session
- 用户退出 SSH 后，任务继续运行
- 用户可通过 `tmux` 重新连接查看任务状态

---

### 7.2.3 功能：分钟级守护进程

#### 需求说明

系统提供一个主守护进程，对所有已登记任务进行周期性巡检。

#### 功能要求

- 守护进程在后台持续运行
- 默认每分钟轮询一次
- 每轮至少检查：
  - 任务对应的 `tmux` session 是否存在
  - 任务进程是否仍在运行
  - 目标 GPU 当前使用情况
- 应记录巡检日志，便于回溯问题

#### 验收标准

- 用户启动一次守护后，无需人工持续干预
- 系统能够在任务异常退出后及时发现状态变化
- 巡检频率可配置，默认值合理

---

### 7.2.4 功能：礼貌型资源抢占与恢复逻辑

#### 需求说明

当守护进程发现某任务未运行时，不应直接重启，而应先判断目标 GPU 是否适合恢复该任务。

#### 判定规则

- 若目标 GPU 当前利用率 `> 5%`，视为“可能有其他人在使用”
- 若目标 GPU 当前利用率 `<= 5%`，视为“可尝试恢复任务”

#### 执行动作

- 当利用率 `> 5%` 时：
  - 不重启任务
  - 将任务标记为等待状态
  - 在后续轮询中持续观察
- 当利用率 `<= 5%` 时：
  - 自动重新拉起对应 `tmux` session 或任务命令
  - 依赖断点续传机制从最近 checkpoint 继续执行

#### 验收标准

- 任务异常退出后，不会在他人正在使用 GPU 时强行恢复
- 目标卡空闲后，系统能自动重新启动任务
- 重启行为可在日志中追踪
- 同一任务不会因状态误判而被重复拉起多个实例

---

## 8. 配置需求

系统需要支持通过配置文件定义任务与调度参数。

### 8.1 配置内容

至少包含以下配置项：

- 任务列表
- 每个任务的名称
- 每个任务绑定的 GPU 编号
- 每个任务的启动命令
- checkpoint / 输出目录
- 守护轮询间隔
- GPU 空闲阈值
- 日志目录
- 自动重启开关

### 8.2 配置目标

- 易读
- 易编辑
- 可版本管理
- 适合多人共享和复用

---

## 9. 用户流程

### 9.1 环境检查流程

1. 用户执行环境检查入口
2. 系统打印 CUDA、驱动、GPU 列表等信息
3. 用户确认机器基本可用

### 9.2 压力测试流程

1. 用户指定目标 GPU
2. 系统启动压力测试
3. 用户在另一个终端观察 `nvidia-smi`
4. 用户根据温度、利用率、稳定性判断机器状态
5. 用户手动停止测试

### 9.3 多卡通信测试流程

1. 用户指定多张 GPU
2. 系统启动通信测试
3. 输出带宽、耗时和测试结果
4. 用户确认多卡环境是否正常

### 9.4 守护任务流程

1. 用户先将自己的任务改造为支持断点续传
2. 用户填写任务配置
3. 用户启动主守护进程
4. 系统创建对应 `tmux` session 并启动任务
5. 守护进程按周期巡检
6. 如果任务退出，则根据 GPU 是否空闲决定等待或恢复

---

## 10. 非功能需求

### 10.1 易用性

- 命令入口应简单直接
- 输出日志应清晰可读
- 文档应包含最小使用示例

### 10.2 稳定性

- 守护逻辑应避免重复启动同一任务
- 异常情况下应尽量保留现场信息
- 状态判断应可解释、可追踪

### 10.3 可维护性

- 模块边界清晰
- 测试、调度、配置、日志相互解耦
- 后续方便扩展更多策略或更多检测项

### 10.4 环境兼容性

- 优先支持常见 Linux 发行版
- 依赖 `nvidia-smi`、`tmux`、`bash`、`python3`
- 支持常见 CUDA 环境和主流 NVIDIA GPU

---

## 11. 依赖与前置条件

### 11.1 系统依赖

- Linux
- NVIDIA GPU 驱动
- CUDA 环境
- `nvidia-smi`
- `tmux`
- `bash`
- `python3`

### 11.2 运行依赖

- 支持 CUDA 的深度学习框架或计算框架
- 用户具备对目标 GPU 设备的基本访问权限

---

## 12. 风险与限制

### 12.1 GPU 利用率判断的局限性

仅通过 GPU 利用率判断“是否有人在使用”，可能存在误判，例如：

- 有人占用显存但当前利用率较低
- 某些任务短时间内处于间歇性低利用率状态

因此，`5%` 阈值应视为默认策略，而不是绝对准确的资源占用判定。

### 12.2 任务可恢复性的前提

自动恢复依赖用户任务本身具备：

- 周期性保存 checkpoint
- 启动时自动恢复
- 安全写入机制

如果任务不满足这些前提，调度器只能重启，不能保证从断点继续。

### 12.3 `tmux` 不是集群调度器

本项目适用于单机共享场景，不负责：

- 全局公平排队
- 跨节点调度
- 优先级抢占
- 多用户权限控制

---

## 13. 成功标准

若项目上线后满足以下条件，则认为第一阶段目标达成：

1. 用户能够在 5 分钟内完成环境检查与压力测试
2. 用户能够通过一个配置文件定义多个后台任务
3. 守护进程能够稳定巡检并识别异常退出
4. 当 GPU 忙碌时，系统不会强行重启任务
5. 当 GPU 空闲时，系统能够自动恢复中断任务
6. 用户可以依赖该工具在 SSH 断开后继续维护长任务运行

---

## 14. 第一阶段交付范围

第一阶段建议交付以下内容：

- 环境检查入口
- 单卡/多卡压力测试入口
- 多卡通信测试入口
- 原子任务模板说明文档
- 基于配置文件的 `tmux` 多任务部署能力
- 分钟级守护逻辑
- GPU 空闲判定与礼貌恢复逻辑
- 基础日志输出能力

---

## 15. 后续可扩展方向

以下内容可作为后续版本扩展：

- 增加显存占用阈值判断
- 支持任务优先级
- 支持任务重启次数限制
- 支持失败告警
- 支持简单 Web 面板或 TUI 面板
- 支持更细粒度的 GPU 状态感知
- 支持多机统一管理
